# تقرير المراجعة - المرحلة 8: مراجعة الكود ومنطق الأعمال (Code Review + Domain Logic Review)

## النطاق (Scope)
- `src/types/*`
- `src/domain/*`

## النتائج والملاحظات

[الخطورة: High] | [src/domain/schemas.ts و schema.sql] | [DRIFT: تعارض في قيم حالة القضية (status) بين Zod Schema وقيود قاعدة البيانات. Zod يسمح بـ "نشطة" بينما DB تفرض "متداولة" أو "محفوظة" أو "مغلقة" أو "تحت الدراسة".] | [كسر كامل لوظيفة إنشاء أو تحديث القضايا، حيث سترفض قاعدة البيانات الحالات غير المتطابقة، مما يؤدي إلى تعطل التطبيق عند محاولة إدخال حالة "نشطة".] | [توحيد الحالات بين Zod وقاعدة البيانات (مثلاً استبدال "نشطة" بـ "متداولة" وإضافة "محفوظة" في Zod Schema).]

[الخطورة: Medium] | [src/domain/financeDomain.ts و src/domain/schemas.ts] | [استخدام العمليات الحسابية العشرية (Floating point) لحساب الضريبة مع سماحية `< 0.01` في التحقق بدلاً من الدقة التامة المطابقة لمتطلبات ZATCA.] | [تراكم أخطاء التقريب قد يؤدي إلى عدم تطابق في التقارير المالية والإقرارات الضريبية مع متطلبات هيئة الزكاة والضريبة والجمارك عند جمع كسور الهللة للعديد من الفواتير.] | [استخدام أرقام صحيحة (Integers) لتمثيل المبالغ بالهللة أو استخدام مكتبة متخصصة (مثل decimal.js) لضمان الدقة التامة بدون الحاجة لاستخدام سماحية تقريبية (Tolerance).]

[الخطورة: High] | [src/domain/legalWorkflow.ts] | [الدالة `mapCaseStatusToStage` تفترض أن أي حالة غير "تحت الدراسة" أو "نشطة" يتم تعيينها كـ "closed" (حالة افتراضية).] | [الحالات الأخرى المعتمدة في قاعدة البيانات (مثل "محفوظة" أو "متداولة") سيتم تصنيفها خطأً كـ "مغلقة"، مما يؤثر سلباً على سير العمل (Workflow) وعرض البيانات بشكل خاطئ في واجهة المستخدم.] | [إضافة تعيين صريح (Explicit Mapping) لكل الحالات الممكنة في النظام، ومعالجة الحالات غير المتوقعة كأخطاء برمجية بدلاً من افتراض أنها مغلقة تلقائياً.]

[الخطورة: Low] | [src/domain/clientDomain.ts و src/domain/schemas.ts] | [تضمين قواعد عمل (Business Rules) محددة جداً للبيئة السعودية بشكل ثابت (Hardcoded)، مثل صيغ الهوية الوطنية، والسجل التجاري، والضريبة، ورقم الجوال.] | [صعوبة تصدير المنصة أو إعادة استخدامها في دول أو مناطق قضائية أخرى (Jurisdictions) دون الحاجة إلى إعادة كتابة أجزاء كبيرة من الكود (تراكم ديون تقنية - Technical Debt).] | [فصل قواعد التحقق الخاصة بكل دولة إلى وحدة إعدادات منفصلة (Configuration/Adapter Pattern) ليتم تفعيلها أو استدعاؤها بناءً على إعدادات المستأجر (Tenant).]

[الخطورة: Low] | [tsconfig.json و src/types] | [تم التحقق من تفعيل النمط الصارم (`strict: true`) وعدم وجود استخدام للنوع `any` بشكل عشوائي في ملفات الأنواع.] | [إيجابي: يحافظ على أمان الأنواع (Type Safety) ويقلل من الأخطاء وقت التشغيل.] | [الاستمرار في تطبيق سياسة عدم استخدام `any` في المستقبل وتفعيل قواعد إضافية للتحقق الصارم إذا لزم الأمر.]

## OUT OF SCOPE — FLAG FOR LATER
- عدم وجود قيد (Check Constraint) في قاعدة البيانات على الحقل `court` في جدول `cases`، رغم أن `Zod Schema` تفرض قائمة محددة سلفاً بأسماء المحاكم.
