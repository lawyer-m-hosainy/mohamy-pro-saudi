# تقرير مراجعة البنية المعمارية (Phase 1: Architecture Review)

## نطاق المراجعة
الطبقات المعمارية: Views → Components → Store → Application → Domain → Services → Repositories → Infrastructure

## النتائج والملاحظات

[الخطورة: High] | [src/views/PortalManagement.tsx, src/views/ClientPortal.tsx, src/views/Login.tsx] | [تجاوز طبقات البنية المعمارية (Layered Architecture) واستدعاء مكتبة Supabase مباشرة من واجهة المستخدم بدلاً من التمرير عبر طبقة Service/Store] | [صعوبة صيانة الكود، تشتت منطق جلب البيانات، وصعوبة اختبار الواجهات بمعزل عن واجهات قواعد البيانات] | [نقل جميع استدعاءات Supabase إلى طبقة الخدمات (Services) أو المستودعات (Repositories) واستخدام مخازن الحالة (Store) لتغذية الواجهات]

[الخطورة: Low] | [src/services/ai/index.ts] | [نمط الواجهة (Facade Pattern) مطبق بشكل سليم ويتم استخدامه باستمرار دون تجاوز في بقية أجزاء التطبيق. التراجع للردود الوهمية (Mock) يعمل بسلاسة وبدون أي استدعاءات جانبية تتخطى الواجهة] | [لا يوجد أثر سلبي، البنية الحالية ممتازة وتضمن استمرارية العمل في حال فشل خادم الذكاء الاصطناعي] | [الاستمرار على هذا النمط المعماري في الخدمات المستقبلية]

[الخطورة: Medium] | [src/repositories/casesRepository.ts, src/services/legalDataService.ts] | [مسارات وصول متوازية (Parallel Access Paths) لبيانات القضايا، حيث تستخدم بعض المكونات `casesRepository` (في طبقة Domain) بينما تستخدم مكونات أخرى مثل `NewCaseDialog` و `App.tsx` (وكذلك `useCaseActions`) خدمة `legalDataService` بشكل مباشر لحفظ وجلب البيانات] | [عدم اتساق في جلب البيانات، صعوبة تتبع التغييرات، وتكرار لمنطق التعامل مع البيانات (Code Duplication)] | [توحيد نقطة الوصول لبيانات القضايا عبر استخدام `casesRepository` حصرياً، وإزالة عمليات القضايا من `legalDataService`]

[الخطورة: Low] | [src/App.tsx] | [تم تطبيق التحميل الكسول (`React.lazy()`) بشكل متسق ومثالي على جميع الواجهات المعروضة (حوالي 40 واجهة) ولا توجد أي واجهة مستوردة بشكل مبكر (Eager Import)] | [تحسين أداء التحميل الأولي للتطبيق بشكل ملحوظ وتقليل حجم الحزمة (Bundle Size)] | [الاستمرار في تطبيق التحميل الكسول لأي واجهات أو وحدات أعمال جديدة يتم إضافتها للتطبيق]

[الخطورة: Low] | [src/store/*] | [لا توجد اعتمادات دائرية (Circular Dependencies) فعلية بين المخازن. تقوم المخازن باستخدام `getState()` لجلب البيانات من المخازن الأخرى بطريقة آمنة. (ملاحظة: تم رصد `useAppStore.ts` كمخزن شامل مدمج ولكن مُصنف كـ Deprecated)] | [لا يوجد أثر حالي، البنية الحالية تمنع التداخل الدائري بين المخازن] | [الاستمرار بتجنب الاعتمادات المتبادلة والاستغناء التدريجي عن `useAppStore` لصالح الاعتماد على المخازن المستقلة]

[الخطورة: Low] | [src/domain/*] | [طبقة النطاق (Domain Layer) نقية تماماً (Pure)؛ لا تحتوي على أي استدعاءات لمكتبات خارجية مثل React أو Supabase أو Zustand، ولم تسرّب أي مكونات من طبقات أخرى] | [سهولة كبيرة في اختبار منطق الأعمال (Business Logic) وقابلية عالية لنقله لأي منصة أخرى مستقبلاً دون تعديل] | [الحفاظ بصرامة على نقاء هذه الطبقة وعدم السماح بأي استيراد (Import) من طبقات البنية التحتية أو الواجهات]

[الخطورة: High] | [src/views/PortalManagement.tsx, src/views/ClientPortal.tsx] | [المكونات تعمل كـ God-Components؛ حيث تقوم بإنشاء وإدارة مستخدمين، جلب البيانات من Supabase، تنفيذ منطق الأعمال، وعمليات التصيير (Rendering) وتخزين الحالات المعقدة في نفس الملف الضخم] | [صعوبة بالغة في القراءة والصيانة، استحالة إعادة استخدام أجزاء من المكون، وازدياد احتمالية ظهور أخطاء متداخلة عند التعديل] | [فصل منطق الأعمال وجلب البيانات إلى Custom Hooks أو تفويضها لـ Zustand Stores، وتقسيم ملف الواجهة إلى مكونات React أصغر ومستقلة]

## OUT OF SCOPE — FLAG FOR LATER
- فحص حجم ملفات الواجهات الكبيرة مثل `Landing.tsx` لتحديد ما إذا كانت بحاجة إلى تقسيم وتحسين (Code Splitting).
